# 技术验证版本记录

Lab 版本独立于原项目 Concept 版本；不修改长期主 GDD。

## Lab v0.3.0 / Render B1 — 2026-09-07

应用内版本与存档版本不变；实验包按源码SHA区分。纯二维地面ArrayMesh一次生成、Transform2D镜头变换、合并网格线；原地面实现保留。修正切换对照模式后的网格缓存失效，以及A/B脚本条件Array类型问题。

源码a4239a76d9605a34320121ba27f2c47ed1103c95，第16次工作流第2次尝试成功。保留所有旧断言，新增2172条零失败；16项Android模拟器系统检查通过。Linux同进程512标记平均35.32→40.55帧/秒，60秒旋转40.87帧/秒；Android软件模拟器仍慢，不能说性能达标或真机/TapPlay通过。第16次第1次尝试为下载连接重置，未跳过校验，重试完整通过。

结果与APK来源：[Render B1](logs/2026-09-07-render-b1-result.md)。旧HTML/GDD未变，正式游戏与公开日志未提前制作。

## Lab v0.3.0 — 2026-09-07

### 新增与修正

- LabLayout：工具栏自适应换行、安全区域映射、完整按钮矩形检查、HOME视野适配。
- LabOcclusion：观察射线与高度/占地相交、三个身体高度采样、L形块状占位体内部面隐藏与淡化。
- FX 0/128/512仅绘制标记负载；地块缓存121项、帧间隔环600项、排版缓存最多32项。
- 排序深度预计算，固定文本排版复用；新增单调时钟帧间隔与分段CPU记录。
- 生命周期测试改用引擎notification派发，保留旧断言，补充暂停/恢复时测量时钟重置。

### 实际验证与问题

最终源码584c63e96a8644789b8cf2be1b7dd7717f430087，第13次工作流34069916135成功。基础262,254、存档/输入51、新适配10,945条断言全部通过；模拟器16项系统检查通过。5尺寸、8遮挡角度、3绘制负载矩阵完成，ARM包签名/架构/权限核验通过。

第11次功能通过但软件模拟器压力场景明显慢，记录保留；第12次生命周期测试派发方式导致3个断言失败，未宣称完整通过；第13次修正派发后整套回归通过。软件模拟器性能仍未达标，不能用测试成功状态掩盖。具体实测数据见logs/2026-09-07-step-03-result.md。

### 仍未验证

ARM实体设备、物理多指、缺口屏、长时发热/功耗、复杂非块状/多层遮挡、TapPlay及其稳定性、正式签名/投稿。仍为赛前技术练习；根目录历史与旧HTML未变。

## Lab v0.2.0 — 2026-09-05

### 新增与修正

- `save_store.gd`：校验封装、临时写入复读、保留有效旧备份、主/备份/临时文件恢复、兼容 v0.1.0。所有文件损坏时保留现场，不用默认状态覆盖。
- `runtime.gd`：启动恢复、周期与暂停保存、失焦停止移动、清理指针状态及误触保护。
- 所有旧存档入口统一调用新存储逻辑，避免基类生命周期通知绕过校验。
- 完全精度 JSON 序列化及还原后的类型规范化，修复移动后存档校验失败。
- 49 条专项断言；新增独立 x86_64 模拟器包与 Android 系统流程自动化。
- 模拟器采用明确 AVD 路径、明确 Activity 启动、开机配置稳定等待和系统全屏提示确认；改用支持的 SwANGLE 渲染后端。
- 原 UI 与场景保持几何占位。没有正式敌人、战斗、生产、卡牌或昼夜内容。

### 真实验收

第 10 次 Android 工作流 `33958879429` 成功：262,254 条基础断言、49 条专项断言全部零失败；ARM 包导出、签名、双架构及无 INTERNET 权限检查通过；Android 15 x86_64 模拟器 11 项检查通过，并人工查看启动/放置/恢复/缩放截图。源代码提交 `d777c4ce076f1a40d728066a2672b574e7a1b8f0`。

### 保留的问题记录

第5次被后续提交取消；第6次存档断言失败；第7次模拟器目录假设错误；第8次启动观察失败；第9次部分逻辑检查虽通过但画面黑屏、Back 检查失败。全部保留为真实历史，不能拿失败运行冒充完整成功。具体见本轮结果文件。

### 尚未验证

ARM 手机、物理多指触控、缺口/安全区和长时负载、TapPlay、官方稳定性检测、正式签名及参赛投稿。旧 HTML 总档案流程未改动。

## Lab v0.1.0 — 2026-09-05

### 新增

- 独立 `android-lab/`，纯二维世界坐标、斜俯视投影、连续水平旋转、缩放与平移。
- 地块点选、占位柱放置/移除、基本深度排序与遮挡淡化。
- 屏幕相对移动、地面寻路、碰撞防穿透、单指/双指输入路由。
- 实验用本地存储及异常数据拒绝，不是正式游戏存档。
- 固定 Godot 4.5.2，自动断言、引擎截图、安卓导出与实际 APK 检查。
- 实验说明、来源披露、设备测试单和证据记录。

### 本轮实际发现并修复

1. 构建环境的 sdkmanager 不在 PATH：改为显式发现其路径，并让源码测试先执行。
2. 自定义 Projection 常量与 Godot 内置类型冲突：改用 LabProjection。
3. Android 导出配置检查无详细错误：检查官方 4.5.2 源码后启用移动端 ETC2/ASTC 纹理导入。

### 通过的验收

第 4 次 Android 工作流成功：262,254 次断言零失败、5 张真实引擎截图、APK 导出、签名检查、ARMv7/ARM64 与无 INTERNET 权限检查。

### 尚未完成

安卓手机实测、TapPlay 启动、TapPlay 稳定性检测、正式签名与商店材料、游戏核心玩法和有效参赛投稿。旧 HTML 总档案的历史迁移工作流仍失败，未在本轮修复。

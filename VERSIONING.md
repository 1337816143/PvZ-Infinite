# 设计文档版本与增量修改规则

## 1. 基本原则

本仓库采用“历史不可丢失、主线增量修订”的维护方式。

1. 原始聊天记录永久保留，不进行润色覆盖。
2. 讨论纪要可以总结，但不能替代原始问答。
3. 主设计稿只修改本轮讨论直接涉及的章节，不因局部调整重写全部框架。
4. 旧方案被否定后，在对应设计章节保留并标注“弃用想法”或“弃用名称”，同时在主设计稿更新当前结论，并在变更日志和原始对话中保留否定原因。
5. 新增内容优先追加到现有正确章节；只有结构确实无法容纳时才新增章节。
6. 不随意移动、重命名或合并既有文件，避免历史链接失效。

## 2. 版本号

当前使用概念阶段版本：`Concept MAJOR.MINOR.PATCH`。

- MAJOR：总体游戏定位或核心类型发生根本变化；
- MINOR：增加完整系统、模式或重要设计支柱；
- PATCH：局部规则、数值、措辞或细节调整。

当前版本：`Concept v0.4.0`。

## 3. 每次变更必须记录

- 日期和轮次；
- 用户提出的新增、异议或修正；
- 修改文件和章节；
- 保持不变的内容；
- 被替代的旧规则；
- 尚未确定的问题；
- 相关图片和其他产物。

## 4. 文件职责

- `docs/conversations/`：逐轮完整原始问答；
- `docs/session-logs/`：每轮讨论摘要与决策；
- `docs/design/00-master-game-concept.md`：当前完整基线；
- `docs/design/`：各系统专题设计；
- `docs/research/`：外部调研；
- `docs/decisions/`：重要设计决策及其理由；
- `assets/`：图片、提示词、哈希和可还原原始文件；
- `CHANGELOG.md`：版本变化；
- `VERSIONING.md`：本规则。

- `AGENTS.md`：AI代理和新对话的最高优先级接手规则；
- `docs/project-rules/`：详细工作流、接手清单和HTML规范；
- `index.html`：GitHub Pages根入口；
- `PvZ-Infinite-Archive.html`：所有仓库内容的单文件无损快照与交互式阅读界面；
- `tools/build_archive.py`：持续生成上述HTML快照。

## 5. 禁止事项

- 不用一份新总结替换所有旧文档；
- 不因措辞优化删除原始细节；
- 不把“待讨论”擅自写成“已确定”；
- 不把不同模式的规则混为一体；
- 不把原始生成图只保存截图而丢失原图数据；
- 不在没有说明的情况下重构目录。
